# Taste (Continuously Learned by [CommandCode][cmd])
- Comfortable with force-pushing destructive changes to production (`git reset --hard` + `git push --force`). Trusts the app has survived worse. Confidence: 0.8
- Prone to making small, pointless iterative tweaks obsessively (self-described: "2 weeks just flipping an arrow"). Confidence: 0.7
- Prefers cleaning up repo bloat by adding to `.gitignore` + `git rm --cached` rather than deleting files outright — keeps the filesystem safe while purging git history. Confidence: 0.8
- Cost-conscious about API/service consumption — values not wasting API credits/balance due to bugs or silent failures in the code. Confidence: 0.8
- Dislikes silent failures — flags "zero console errors" as a red flag; wants errors to be visible and logged rather than swallowing them. Confidence: 0.7
- Expects every permission defined in the permission system to map to an actual implemented feature — flags "the permission exists but there's no feature" as a gap to fix by building the feature behind it. Confidence: 0.6
- Frames sensitive/destructive user actions (e.g., account deletion) as a *request* flow that an admin reviews and approves, rather than instant self-service deletion — user explicitly asked to "request deleting their account," and the implementation added a pending-request status plus an admin approval/reject panel. Confidence: 0.6
- Maintains a dedicated `staging` git remote as a test/deploy target: pushes current changes there for verification (pulling/merging staging's newer commits first when it's ahead, then pushing) rather than deploying straight to production. Confidence: 0.9
- Ships releases to BOTH environments at once — when a fix set is ready, explicitly asks to "push it to both prod and staging (main + staging)", expecting the same changes deployed to the prod `main` branch and the `staging` branch in the same release cycle, not staging as a gate before prod. Confidence: 0.7
- Prefers testing against the already-live hosted staging deployment (e.g., `legobele.github.io/nivelato-staging`) rather than the agent spinning up local servers or local test copies — explicitly called out the agent for serving localhost:4173 when the real app was already live on staging. Confidence: 0.7
- When shipping to prod, prefers to bundle all working-tree changes (including uncommitted in-progress feature work) into one push rather than committing only a minimal scoped fix — picked "everything + fix" over a fix-only push. Confidence: 0.5
- Communicates casually/informally when describing feature work (slang, filler like "so uh", "and shit", lowercase text, emoji like 😭) rather than formally — including playful banter with the AI, sarcasm markers like "/s", and all-caps hype ("PUSH THAT BITCH LETS GOOOOOO"). Confidence: 0.98
- Enjoys when the AI matches their meme energy and playfully roasts/memes their own files and old projects (e.g., inviting the AI to "torture the vision model" with a cringe 12-year-old startup pitch deck) rather than responding with dry, clinical analysis. Confidence: 0.5
- Communicates feature ideas via quick, crude visual mockups (e.g., an MS Paint sketch of a graph) rather than polished specs or lengthy descriptions — the rough image is the requirement. Confidence: 0.5
- Wants new features to tie into existing cloud/backup infrastructure rather than requiring separate services — asked whether annotated photos stored in Firebase Storage connect to the existing cloud backups or would need an extra CDN, and prefers the built-in cloud path over added infra. Confidence: 0.4
- Explicitly-shared content should be visible to the recipient even when they lack the general "view others'" permission — per-item sharing (e.g., a `sharedWith` list) overrides broad permission denials. Confidence: 0.5
- Expects the agent to run deploys/builds itself using the user's installed CLI tooling (e.g., Firebase CLI) rather than handing commands back for the user to execute manually. Confidence: 0.4
- Wants the app actually tested end-to-end after shipping a feature — explicitly asked to "TEST THE APP" (all caps, framed as a bonus step) and picked having the agent drive the real app with Playwright against live Firebase (offering to paste test credentials so the agent can run it) rather than skipping testing or handing the commands back. Confidence: 0.7
- When asking for tests, supplies a concrete real-world artifact (e.g., an actual annotated glass-door photo) and says "test the full flow with this" — the attached fixture IS the intended test input, and the expectation is a full user-journey test (login → measurement steps → photo annotation → save → dashboard) driven by it rather than a synthetic stand-in. Confidence: 0.5
- Ships immediately once the green light is given: after a feature passes the e2e suite, wants an instant push to production — responded to "everything passed" with an emphatic all-caps "PUSH THAT BITCH LETS GO" and expected the agent to deploy right away (no extra verification or second-guessing). Confidence: 0.8
t) rather than skipping testing or handing the commands back. Confidence: 0.6
- Wants playful hidden easter eggs baked into the production app, requested spontaneously as "bonus additions" (e.g., typing "nivelato" with no field focused makes the whole UI go full Italian) — and expects them to ship alongside real functionality, not as throwaway hacks. Confidence: 0.7
- Reports UI bugs by attaching a screenshot with a terse, lowercase caption that states just the symptom (e.g., "the left side is rounded, the right side isn't. also RE: the image isnt showing up") and expects the agent to visually inspect the image and diagnose/fix it. Often circles/highlights the offending element in the screenshot itself ("this thing highlighted on every one of the sections. remove it") to disambiguate exactly what to remove. Confidence: 0.6
- For hidden keyboard triggers / easter-egg keywords, wants them to fire only when no form field is focused (never hijack typing inside inputs) and to work across every page of the app ("any part"), not just the main screen. Confidence: 0.6
- Runs local LLM experiments (e.g., small quantized Gemma agent debates) on their own machine via LM Studio rather than cloud APIs. Confidence: 0.6
- Shares raw output of their local LLM experiments (e.g., a 2B Gemma's 6.84-second thinking trace before replying "hi") as entertainment, with no framing or commentary — expects the AI to riff on the model's quirks with meme energy rather than analyze it technically. Confidence: 0.4
- Specifies custom UI/features by referencing familiar, proven interaction patterns (e.g., "like one of those document photo based scanning tools", "a little zoom glass typa thing") — expects the agent to model custom UI on those established patterns rather than inventing bespoke interactions. Confidence: 0.6
- When the user is stuck or uncertain about the semantics of a measurement/geometry (e.g., which direction a lean indicator should point), wants the agent to reason it out objectively and give a definitive answer rather than bounce the question back for confirmation — explicitly asked "objectively, which side should it go to, my brain might be fucking with me." Confidence: 0.7
- When the user declares a measurement indicator is wrong on BOTH sides (e.g., "BOTH SIDES ARE WRONG"), treats it as evidence the app's whole convention is inverted and expects the agent to hunt the shared root cause — one shared calc/function driving both walls, or misread input semantics — rather than flip each side independently or defend the prior conclusion. BUT such frustrated directional claims are unreliable: the user retracted a confident "both sides wrong" verdict minutes later ("Im fucking stupid... Revert the goddamn changes") and the app's original arrow convention turned out to be correct — so confirm against the live app/diagrams before committing a direction flip. Confidence: 0.4
- Verifies fixes against the live deployed app directly (e.g., "I'm checking the live app") rather than trusting code reading — the running app is the source of truth, and bug reports come as symptoms observed in it. Confidence: 0.6
nd expects them to ship alongside real functionality, not as throwaway hacks. Confidence: 0.7
- For hidden keyboard triggers / easter-egg keywords, wants them to fire only when no form field is focused (never hijack typing inside inputs) and to work across every page of the app ("any part"), not just the main screen. Confidence: 0.6
 focused (never hijack typing inside inputs) and to work across every page of the app ("any part"), not just the main screen. Confidence: 0.6
- Uses bare "." messages and terse single-word slang imperatives (e.g., "cook") as pings to wake the agent and prompt it to proceed when it's idle, stalled, or hasn't responded — the single period/word means "get going on the queued backlog" (e.g., "cook" right after a token-limit error interrupted the session meant continue and finish the job); expects the agent to parse and execute the accumulated work in one pass without asking for clarification on each item. Confidence: 0.7
- Commits in the repo intentionally carry a "Co-authored-by" trailer (CommandCodeBot + the "vibes" co-author list), stamped by a custom `prepare-commit-msg` git hook — this trailer is a desired part of the commit history; if the hook breaks (e.g., python missing on PATH), bypass it temporarily (--no-verify / move the hook aside) to unblock the commit but restore it afterwards so future commits keep the trailer. Confidence: 0.6
- Photo annotation belongs inside the measurement flow as an in-app experience — explicitly "dashboard should NOT link to /photos.html": no standalone photo-editor page reachable from elsewhere in the app. Confidence: 0.5
- Reports batches of UI bugs as terse bullet points prefixed with "-->"/"--->" (e.g., "--> Lines and arrows don't persist if no text is placed", "---> The magnifier isn't working properly") and expects them all handled in one pass without per-item clarification. Confidence: 0.5
- Deploys to production with a terse "push to prod" command — expects the agent to autonomously commit all pending app changes and push to `main` (prod = GitHub Pages), then notify that Pages will rebuild and to hard-refresh; no staging gate or extra confirmation unless the user asks for it. Confidence: 0.6
- Wants the same interactive component/engine shared across all views of the app rather than per-page duplicate copies — explicitly asked to "make the same graph system we use from the main app work on the dashboard's job modal", expecting the shared pan/zoom graph engine (single source of truth, deleted the dashboard's static duplicate) instead of a separate static renderer per page. Confidence: 0.6
